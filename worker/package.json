{
  "name": "mytafrit-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Mytafrit OG tags injection",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
